[project]
name = "django-weekly-planner"
version = "0.1.0"
description = "A time-blocking weekly planner built with Django — click any cell, type a label, pick a color, and drag to resize."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
docker = [
    "gunicorn>=26.0.0",
    "whitenoise>=6.12.0",
]

[tool.ruff]
line-length = 99
target-version = "py312"
extend-exclude = ["*/migrations/*"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
